# To add a new source, copy the structure below and replace the key ("albertatheatreprojects") 
# with the source identifier (usually the organization's slug or name). 
# Each source can define its own attributes, such as:
#   - attribute_EventUrl: the URL where events are listed
#   - offerUrl: a booking/purchase URL that may include placeholders (e.g., {webInstanceId})
# Add additional attributes as needed for that source.
#
# Example structure:
#
# sourcename1:
#   attribute_EventUrl: "https://example.com/events1"
#   offerUrl: "https://example.com/book/?id={eventId1}"
#
# sourcename2:
#   attribute_EventUrl: "https://example.com/events2"
#   offerUrl: "https://example.com/tickets/?event={eventId2}"

# Transformation functions available as of now
# extractID:
#   Extracts numeric ID from a string (e.g., "instance_12345" -> "12345")
#   example usage: {extractID(name)}
# slugify:
#   Converts a string to a URL-friendly slug (e.g., "My Event Name" -> "my-event-name")
#   example usage: {slugify(name)}
#   additional kwargs can be passed like: {slugify(name, remove_words=['the','over'])}
#   ADDITIONAL ARGUMENTS MUST BE NAMED ARGUMENTS

albertatheatreprojects:
  attribute_EventUrl: "https://albertatheatreprojects.com/whats-on/"
  offerUrl: "https://albertatheatreprojects.com/book/?web_instance_id={instance_webInstanceId}"
  attribute_PresentedBy: "Alberta Theatre Projects"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-6382"

honens:
  attribute_EventUrl: "https://www.honens.com/events/"
  attribute_PresentedBy: "Honens"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-6383"

kaymeekcentre:
  attribute_EventUrl: "https://kaymeek.com/events/"

manitobaopera:
  offerUrl: "https://purchase.mbopera.ca/ChooseSeats/{extractID(instance_id)}"
  attribute_PresentedBy: "Manitoba Opera"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-5133"

nacc:
  attribute_EventUrl: "https://naccnt.ca/events/{slugify(name)}/"
  offerUrl: "https://purchasing.naccnt.ca/ChooseSeats/{extractID(instance_id)}"

sidwilliams:
  attribute_EventUrl: "https://www.sidwilliamstheatre.com/events/{slugify(name)}/"
  offerUrl: "https://www.sidwilliamstheatre.com/choose-seating/?id={extractID(instance_id)}"

symphonynovascotia:
  attribute_PresentedBy: "Symphony Nova Scotia"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-5271"

theporttheatre:
  offerUrl: "https://www.porttheatre.com/choose-seats/?id={extractID(instance_id)}"

theisabel:
  attribute_EventUrl: "https://www.queensu.ca/theisabel/whats-on/{slugify(name, remove_words=['the','over'])}/" 

earlymusicvancouver:
  attribute_EventUrl: "https://www.earlymusic.bc.ca/events/{slugify(name)}/"
  attribute_PresentedBy: "Early Music Vancouver"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K10-351"

tidemarktheatre:
  attribute_EventUrl: "https://tidemarktheatre.com/event/{slugify(name)}/"

improvcentrevancouver:
  attribute_EventUrl: "https://theimprovcentre.ca/shows/"
  offerUrl: "https://purchase.theimprovcentre.ca/ChooseSeats/{extractID(instance_id)}"
  attribute_PresentedBy: "The Improv Centre"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-6384"

rcmusic:
  offerUrl: "https://www.rcmusic.com/tickets/seats/{extractID(instance_id)}"

vancouverrecitalsociety:
  offerUrl: "https://purchase.vanrecital.com/EventAvailability?WebEventId={webEventId}"
  attribute_PresentedBy: "Vancouver Recital Society"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K10-348"

ucalgaryuts:
  offerUrl: "https://theatre-tickets.ucalgary.ca/?tx={extractID(id)}"

gallery7:
  attribute_EventUrl: "https://www.gallery7theatre.com/shows-training/{slugify(name)}/"
  offerUrl: "https://www.gallery7theatre.com/shows-training/{slugify(name)}/performances"
  attribute_PresentedBy: "Gallery 7 Theatre"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-6374"

tarragontheatre:
  offerUrl: "https://purchase.tarragontheatre.com/EventAvailability?EventId={extractID(id)}&ref=bookNow&scroll=timeAndDates"
  attribute_PresentedBy: "Tarragon Theatre"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-6114"

calgaryphil:
  attribute_PresentedBy: "Calgary Philharmonic Orchestra"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-5588"

metropolitancooperativetheatresociety:
  offerUrl: "https://purchase.metrotheatre.com/ChooseSeats/{extractID(instance_id)}"
  attribute_PresentedBy: "Metro Theatre"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-6149"

capilanouniversity:
  offerUrl: "https://purchase.capilanou.ca/EventAvailability?EventId={extractID(id)}"
  attribute_PresentedBy: "BlueShore Financial Centre for the Performing Arts"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K2-5389"

yukonartscentre:
  offerUrl: "https://purchasing.yukonartscentre.com/ChooseSeats/{extractID(instance_id)}"

theatreaquarius:
  offerUrl: "https://purchasing.theatreaquarius.org/EventAvailability?EventId={extractID(id)}&ref=bookNow&scroll=timeAndDates"

owensoundlittletheatre:
  attribute_EventUrl: "https://roxytheatre.ca/shows/{slugify(name)}/"
  offerUrl: "https://purchase.roxytheatre.ca/EventAvailability?EventId={extractID(id)}&ref=bookNow&scroll=timeAndDates"

nationalmusiccentre:
  offerUrl: "https://purchase.nmc.ca/EventAvailability?EventId={extractID(id)}"

centaurtheatre:
  attribute_PresentedBy: "Centaur Theatre"
  attribute_PresentedBySameAs: "http://kg.artsdata.ca/resource/K10-32"

eastlinkcentre:
  offerUrl: "https://purchase.eastlinkcentrepei.com/EventAvailability?EventId={extractID(id)}"

thousandislandsplayhouse:
  attribute_EventUrl: "https://www.1000islandsplayhouse.com/{slugify(name)}/"
  offerUrl: "https://www.1000islandsplayhouse.com/book-tickets/?book_id={extractID(instance_id)}"
